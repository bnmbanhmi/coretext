[project]
name = "trore"
version = "0.1.0"
description = "Real estate platform monorepo"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
dev-dependencies = []

[tool.uv.workspace]
members = ["apps/api", "packages/importer"]
