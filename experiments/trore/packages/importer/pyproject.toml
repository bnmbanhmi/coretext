[project]
name = "importer"
version = "0.1.0"
description = "Trore Data Importer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
