{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build for API'",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "dev": "uv run uvicorn app.main:app --reload --port 8000"
  }
}
