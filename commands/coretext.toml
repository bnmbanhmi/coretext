[[commands]]
name = "status"
title = "CoreText Status"
description = "Check the health and status of the CoreText daemon and database"
command = "python3"
args = ["-m", "coretext.main", "status"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "init"
title = "Initialize CoreText"
description = "Initialize a new CoreText project"
command = "python3"
args = ["-m", "coretext.main", "init"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "start"
title = "Start CoreText Daemon"
description = "Starts the CoreText daemon (SurrealDB and FastAPI server) in the background"
command = "python3"
args = ["-m", "coretext.main", "start"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "stop"
title = "Stop CoreText Daemon"
description = "Stops the CoreText daemon"
command = "python3"
args = ["-m", "coretext.main", "stop"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "lint"
title = "Lint Markdown Files"
description = "Runs integrity checks on Markdown files via the daemon"
command = "python3"
args = ["-m", "coretext.main", "lint"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "sync"
title = "Sync Files"
description = "Manually synchronizes markdown files in the specified directory to the graph"
command = "python3"
args = ["-m", "coretext.main", "sync"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "apply-schema"
title = "Apply Database Schema"
description = "Applies the schema from .coretext/schema_map.yaml to the local SurrealDB"
command = "python3"
args = ["-m", "coretext.main", "apply-schema"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "new"
title = "Create New File from Template"
description = "Generates a new Markdown file from a BMAD template"
command = "python3"
args = ["-m", "coretext.main", "new"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "install-hooks"
title = "Install Git Hooks"
description = "Installs Git hooks for coretext synchronization"
command = "python3"
args = ["-m", "coretext.main", "install-hooks"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "inspect"
title = "Inspect Dependency Tree"
description = "Inspects the dependency tree of a specific node"
command = "python3"
args = ["-m", "coretext.main", "inspect"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }

[[commands]]
name = "ping"
title = "Ping"
description = "Simple ping-pong check"
command = "python3"
args = ["-m", "coretext.main", "ping"]
cwd = "${extensionPath}"
env = { PYTHONPATH = "${extensionPath}" }